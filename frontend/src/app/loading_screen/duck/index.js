import reducer from './reducers';
export { default as loadingOperations } from './operations';
export { default as loadingActions } from './actions';
export { default as loadingTypes } from './types';

export default reducer;