const TAKE_TURN = "TAKE_TURN";
const WIN = "WIN";
const DRAW = "DRAW";
const RESET = "RESET";
const SET_GAME_STATE = "SET_GAME_STATE";

export default {
    TAKE_TURN,
    WIN,
    DRAW,
    RESET,
    SET_GAME_STATE
}